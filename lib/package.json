{
  "name": "@janwilmake/uithub",
  "version": "0.0.3",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc --outDir build --declaration --module esnext --moduleResolution bundler --target esnext src/index.ts src/parse-zip.ts src/format.ts src/types.ts",
    "pub": "npm publish --access=public"
  },
  "dependencies": {
    "yaml": "^2.8.2"
  }
}
