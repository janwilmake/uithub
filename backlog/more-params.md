# params

Add search via `?q=xxx` that looks in the file contents for a case insensitive match. If it includes `path:???` that could be a path search. This way we can search for specific files anywhere too.

Update OpenAPI

# better header

- it needs all filters from the api that are useful.
- it needs search

But why? maybe we bypass this directly with a message.
